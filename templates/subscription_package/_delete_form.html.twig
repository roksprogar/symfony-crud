<form method="post" action="{{ path('app_subscription_package_delete', {'id': subscription_package.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscription_package.id) }}">
    <input type="submit" value="Delete" class="btn btn-danger">
</form>